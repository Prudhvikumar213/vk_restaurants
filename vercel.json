{
  "version": 2,
  "builds": [
    { "src": "api/app.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/app.py" }
  ],
  "env": {
    "MONGODB_URI": "@mongodb_uri",
    "FLASK_SECRET_KEY": "@flask_secret_key"
  }
}
