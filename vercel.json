{
  "version": 2,
  "builds": [{
    "src": "app.py",
    "use": "@vercel/python"
  }],
  "routes": [{
    "src": "/(.*)",
    "dest": "app.py"
  }],
  "env": {
    "FLASK_SECRET_KEY": "your-secret",
    "MONGODB_URI": "your-uri",
    "DATABASE_NAME": "your-db"
  }
}
